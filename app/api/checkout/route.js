export async function POST(request){try{const key=process.env.STRIPE_SECRET_KEY;const body=await request.json().catch(()=>({}));const plan=body.plan==='full'?'full':'basic';const price=plan==='full'?process.env.STRIPE_FULL_PRICE_ID:process.env.STRIPE_BASIC_PRICE_ID;const appUrl=process.env.NEXT_PUBLIC_APP_URL||new URL(request.url).origin;if(!key||!price)return Response.json({error:'Billing is not configured. Add STRIPE_SECRET_KEY, STRIPE_BASIC_PRICE_ID and STRIPE_FULL_PRICE_ID in Vercel.'},{status:503});const form=new URLSearchParams();form.set('mode','subscription');form.set('line_items[0][price]',price);form.set('line_items[0][quantity]','1');form.set('success_url',`${appUrl}/api/billing/activate?session_id={CHECKOUT_SESSION_ID}`);form.set('cancel_url',`${appUrl}/pricing?checkout=cancelled`);form.set('allow_promotion_codes','true');form.set('billing_address_collection','auto');form.set('customer_creation','always');form.set('subscription_data[metadata][product]','tialo-academic-coach');form.set('subscription_data[metadata][plan]',plan);if(typeof body.email==='string'&&body.email.trim())form.set('customer_email',body.email.trim());const r=await fetch('https://api.stripe.com/v1/checkout/sessions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/x-www-form-urlencoded'},body:form.toString()});const data=await r.json();if(!r.ok)return Response.json({error:data?.error?.message||'Unable to start checkout.'},{status:r.status});return Response.json({url:data.url})}catch{return Response.json({error:'Unable to start checkout.'},{status:500})}}
